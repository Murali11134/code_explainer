[project]
name = "code-explainer"
version = "0.1.0"
description = "Stage 1 static code flow explainer"
requires-python = ">=3.10"

[project.scripts]
code-explainer = "code_explainer.cli:main"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
